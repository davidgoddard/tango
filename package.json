{
  "name": "tanda-player-typescript",
  "version": "1.0.0",
  "description": "Tanda Player Lite",
  "main": "app.js",
  "type": "module",
  "dependencies": {
    "tsc": "^2.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.1",
    "http-server": "^14.1.1",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "/usr/local/bin/tsc && node esbuild.js && npm run build:cp-public",
    "build:cp-public": "cp -R ./src/non-ts/** ./dist",
    "type": "/usr/local/bin/tsc --noEmit",
    "start": "npx http-server dist --cors"
  },
  "author": "David Goddard",
  "license": "ISC"
}
